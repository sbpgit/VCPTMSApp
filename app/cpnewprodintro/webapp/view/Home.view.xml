<mvc:View xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form" controllerName="cpapp.cpnewprodintro.controller.Home"
    xmlns:html="http://www.w3.org/1999/xhtml"
    xmlns:t="sap.ui.table">
    <Page id="idClassChar">
        <customHeader>
            <Toolbar >
                <Title id="profTitle" text="{i18n>title}" class="boldText"></Title>
                <ToolbarSpacer/>
                <Button id="idAdd" text="Create" icon="sap-icon://add" press="onCreate" tooltip = "Create new product"/>
                <Button id="idUpdate" text="Update" icon="sap-icon://edit" press="onCreate" tooltip = "Update product"/>
            </Toolbar>
        </customHeader>

        <content>
            <SearchField id="idSearch" liveChange="onSearch" placeholder="Product"/>
            <Table id="ProdList" items="{path: '/Struresults'}" mode="SingleSelectMaster">
                <columns>
                    <Column hAlign="Center">
                        <Text text="{i18n>StruNode}" />
                    </Column>
                    <Column hAlign="Center">
                        <Text text="{i18n>nodeDesc}"/>
                    </Column>
                    <Column hAlign="Center">
                        <Text text="{i18n>lower}"/>
                    </Column>
                    <Column hAlign="Center">
                        <Text text="{i18n>upper}"/>
                    </Column>
                    <Column hAlign="Right">
                        <Text text="" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{CHILD_NODE}" />
                            <Text text="{NODE_DESC}" />
                            <Text text="{LOWERLIMIT}" />
                            <Text text="{UPPERLIMIT}" />
                            <Button id="idStru" icon="sap-icon://decline" tooltip="Delete" press="onStruNodeDel" iconDensityAware="false" type="Transparent"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>

        </content>
    </Page>
</mvc:View>