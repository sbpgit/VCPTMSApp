VIEW CP_V_PRIMARY_AGG AS
SELECT CAL_DATE, LOCATION_ID, PRODUCT_ID, SUM(PRIMARY_QTY) AS PRIMARY_AGG, "TYPE"
FROM CP_V_PRIMARY WHERE LOCATION_ID = 'PLSE'
GROUP BY CAL_DATE, LOCATION_ID, PRODUCT_ID, "TYPE"
ORDER BY CAL_DATE, LOCATION_ID, PRODUCT_ID, "TYPE";