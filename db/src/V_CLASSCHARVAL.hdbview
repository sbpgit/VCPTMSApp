VIEW "V_CLASSCHARVAL" AS
SELECT  A.CLASS_NUM,
        A.CLASS_NAME,
        B.CHAR_NUM,
        B.CHAR_NAME,
        B.CHAR_VALUE,
        B.CHARVAL_NUM
        FROM CP_CLASS AS A
        INNER JOIN "V_CHARVAL" AS B
        ON A.CLASS_NUM = B.CLASS_NUM;