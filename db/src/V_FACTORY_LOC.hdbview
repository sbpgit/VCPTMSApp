----------------------------------------------------------------------------------------
-- Name:            V_FACTORY_LOC
-- Description:     View to fetch location description
-- Join Details:    A: CP_FACTORY_SALESLOC -  Table for Factory Locations
--                  B: CP_LOCATION - To fetch the location description
-----------------------------------------------------------------------------------------
VIEW "V_FACTORY_LOC" AS
SELECT DISTINCT
A.FACTORY_LOC,
B.LOCATION_DESC
FROM "CP_FACTORY_SALESLOC" AS A
Inner JOIN "CP_LOCATION" AS B
ON A."FACTORY_LOC" = B."LOCATION_ID";