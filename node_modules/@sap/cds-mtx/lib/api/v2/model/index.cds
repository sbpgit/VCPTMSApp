
type TenantID: String;
type CSN: { };
type XML: String;
type Locale: String;


/**
 * Used by CAP runtimes to retrieve tenant-specific variants of
 * deployed models used to serve requests.
 */
service ModelProviderService @(requires: ['authenticated-user','system-user']) {

  action csn(tenant:TenantID, version:String, toggles: array of String) returns CSN;

  action edmx(tenant:TenantID, version:String, toggles: array of String, service:String, locale:Locale, odataVersion:String) returns XML;
}