module.exports = class QueueOverflowError extends Error {
    constructor(currentLength, maxLength) {
        super(`Queue overflow. Current length ${currentLength}, max length ${maxLength}`);
        this.code = this.statusCode = 429;
    }
};
