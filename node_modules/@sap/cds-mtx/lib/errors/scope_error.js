module.exports = class ScopeError extends Error {
    constructor(scope) {
        super(`Forbidden. The scope necessary for this operation is not assigned: ${scope}`);
        this.code = this.statusCode = 403;
    }
};
