module.exports = class TenantCorruptedError extends Error {
    constructor(tenantId) {
        super(`Tenant ${tenantId} exists, but is corrupted. Please offboard the tenant. CAUTION: this will irreversibly delete the container.`);
        this.code = this.statusCode = 422;
    }
};
