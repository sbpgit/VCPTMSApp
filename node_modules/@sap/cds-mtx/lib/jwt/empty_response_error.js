module.exports = class EmptyResponseError extends Error {
    constructor(jwtUrl, response) {
        super();
        this.error_description = `Could not get authentication token from ${jwtUrl}; please check the application url or subdomain. Response: ${response}`;
    }
};
